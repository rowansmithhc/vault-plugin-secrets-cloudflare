name: Go

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.21.4'

    - name: Build
      run: CGO_ENABLED=0 go build -o vault-plugin-secrets-cloudflare cmd/cloudflare/main.go

  release-linux-amd64:
    name: release linux/amd64
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: wangyoucao577/go-release-action@v1
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        goos: linux
        goarch: amd64
        goversion: 1.21.4
        overwrite: true
        pre_command: export CGO_ENABLED=0 && go get github.com/rowansmithhc/vault-plugin-secrets-cloudflare/tree/main/cmd/cloudflare
  release-linux-arm64:
    name: release linux/arm64
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: wangyoucao577/go-release-action@v1
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        goos: linux
        goarch: arm64
        pre_command: export CGO_ENABLED=0
        build_flags: cmd/cloudflare/main.go

  release-linux-amd64-default:
    name: release linux/amd64-default
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: wangyoucao577/go-release-action@v1
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        goos: linux
        goarch: amd64
        pre_command: export CGO_ENABLED=0 && go get github.com/rowansmithhc/vault-plugin-secrets-cloudflare/tree/main/cmd/cloudflare
